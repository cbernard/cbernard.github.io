---
export interface Props {
  translate?: boolean;
  progress?: "scroll" | "load";
}

const { translate = false, progress = "scroll" } = Astro.props;
---

<div
  class="flex h-full"
  class:list={["flex h-full", { "items-stretch": translate }]}
  x-data={`progress({ type: '${progress}' })`}
  x-bind:style="{ '--offset': offset }"
>
  <div x-ref="container">
    <div
      class:list={[
        {
          "transition-translate translate-y-(--offset) ease-in-out": translate,
        },
      ]}
    >
      <number-flow x-ref="number"></number-flow>
    </div>
  </div>
</div>

<!-- <script>
  import Alpine from "alpinejs";

  import progress from "./progress";

  Alpine.data("progress", progress);
</script> -->
